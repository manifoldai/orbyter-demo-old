global:
  raw_data_dir: "/mnt/data/tests/raw"
  processed_data_dir: "/mnt/data/tests/processed"

model:
  # model name. see factory.py for options
  model_name: "random_forest"
  # model params
  model_params: {}
  # dir to save model weights
  model_path: "/mnt/data/tests/model_cache/train_output_model.pkl"

train:


evaluate:
  # Flag to retrain the model or not. If False, then the cached model from model->model_dir will be used as model.
  retrain: True
  # MLFlow experiment name
  experiment_name: "test"

predict:
  model_path: "/mnt/data/tests/model_cache/predict_model.pkl"
  data_path: "/mnt/data/tests/processed/X.pqt"
  predictions_path: "/mnt/data/tests/predictions/yhat.pqt"
